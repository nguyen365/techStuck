#include "dataWriter.h"

DataWriter::DataWriter()
{
  /*std::string buffer;
  std::fstream fs;
  fs.open("setting.ini", std::fstream::in);
  if (fs.is_open())
  {
    fs >> buffer;
    if (buffer == "
  }*/
  terrainFile = "terrainType.txt";
}